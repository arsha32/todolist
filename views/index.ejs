<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css"
      integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO"
      crossorigin="anonymous"
    />
    <link rel="icon" href="icon.ico"/>
    <link rel="stylesheet" href="style.css" />
    <title>TODO LIST</title>
  </head>
  <body>
    <div class="head"><h1>TODOs List</h1></div>

    <div class="container my-3">
      <h4 class="text-center">Home</h4>
      <form  action="/data" method="post">
        <div class="form-group">
          <label for="title">Title</label>
          <input
            type="text"
            class="form-control"
            id="title"
            aria-describedby="emailHelp"
            placeholder="Add Keyword"
            name="titlevalue"
            required
          />
        </div>
        <div class="form-group">
          <label for="description">Description</label>
          <textarea class="form-control" name="desvalue" id="description" rows="3" required></textarea>
        </div>
        <button
          id="add"
          class="btn"
          style="background-color: blueviolet; color: white"
        >
          Add to list item
        </button>
      </form>
      <div id="items" class="" my-4>
        <h3>Your Items</h3>
        <table  class="table styled-table" >
          <thead>
            <tr>
              <th scope="col">sno</th>
              <th scope="col">Tilte</th>
              <th scope="col">Description</th>
              <th scope="col">Actions</th>
            </tr>
          </thead>
          <tbody id="tableBody" >
            
            <% for(var i=0;i<title.length;i++){ %>
            <tr >
                    <td scope="row"><%= i+1 %></td>
                    <td><%= title[i] %></td>
                    <td><%= description[i] %></td>
              <td>
                
        <form action="/delete" method="post">
            <input type="hidden" name="titledelete" value="<%= title[i] %>">
                <button 
                class="btn-delete"
                >
                  Delete
                </button>
                </form>
              </td>
            </tr>
            <%}%>
          </tbody>
        </table> 
      </div>
    </div>
    <footer class="foot">
      <h3>&copy<a href="#">Harsha</a></h3>
    </footer>
  </body>
</html>
